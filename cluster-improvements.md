# Cluster improvements registry

Running list of cluster issues, tech-debt items, and opportunities discovered during the **DevOps tutor track** (and other reviews). Single source of truth — append, don't fork.

Indexed from `~/.claude/library.md`. Source-of-truth path: `user@192.168.100.52:~/homelab-docs/cluster-improvements.md`.

**Severity:** L (low / cosmetic) · M (medium / paper cut) · H (high / actively risky).
**Status:** open · in-progress · deferred · tracked-elsewhere · done.

---

## Open items

| # | Date found | Sev | Topic | Finding | Proposed fix | Status |
|---|-----------|----:|-------|---------|--------------|--------|
| 1 | 2026-05-04 | H | k8s version skew | Phones (one61/62/6t) at k3s v1.35.0; k3master + k3frigate at v1.34.3. Reversed skew (workers ahead of control plane). | Upgrade control plane to v1.35.x **before** any further worker change. Order: master -> k3frigate -> phones (already done). Verify with `kubectl get nodes` post-upgrade. | open |
| 2 | 2026-05-04 | M | Phone-to-cluster network split-brain | Cross-node pod traffic to phone Pod IPs from k3master/k3frigate broken since 2026-04-29 node-ip flip. | Already documented in `~/.claude/skills/k8s-ops/references/network.md`. Resolve as part of Phase 2 HA rollout (per `phase2-plan.md`). | tracked-elsewhere |
| 3 | 2026-05-04 | M | No role labels on phones | one61/62/6t have only built-in labels. App placement is by chance, not policy. | Add `homelab/role=mqtt|api|frontend` labels per node; add `nodeSelector` to relevant Deployments after labels stabilize. Reversible. | open |
| 4 | 2026-05-04 | M | `default` namespace is a grab-bag | gitea, loki, uptime-kuma, open-webui, guitar-backend/frontend all live in `default`. No quotas, no clear ownership. | Migrate each to a dedicated namespace (`gitea`, `logging`, `uptime-kuma`, `open-webui`, `guitar`). Move incrementally; update Service DNS references. | open |
| 5 | 2026-05-04 | M | gpu-inference worker not on GPU node | `mqtt-inference-worker-*` Pod runs on `one62` (no GPU). The `nvidia.com/gpu.present=true` label is on `k3frigate`. | Add `nodeSelector: nvidia.com/gpu.present: "true"` (or proper resource request `nvidia.com/gpu: 1`) to Deployment. Verify Pod re-schedules to k3frigate. | open |
| 6 | 2026-05-04 | L | Stale Errored Pods cluttering listings | `open-webui-7cc6bcffd4-98m7d` (Error 60d, no IP), `frigate-backup-test-1777460770-*` (Error 4d17h), `opencl-test` (Error 24d). | One-time cleanup with `kubectl delete pod ...` once confirmed stale. Add CronJob or `ttlSecondsAfterFinished` for future Job runs. | open |
| 7 | 2026-05-04 | M | Single control plane = SPOF | k3master is the only control plane. Loss = no scheduling, no kubectl until rebuild. | Phase 2 plan (`phase2-plan.md`) targets 3-node etcd HA. SP4 (`k3node2`) not yet provisioned. | tracked-elsewhere |
| 8 | 2026-05-04 | L | Control plane runs app workloads | `chickenflow-backend` deliberately pinned to k3master via `chickenflow/storage=true`. Acceptable for homelab; in production CPs are tainted. | Document as deliberate deviation; revisit if/when CP is re-tainted during HA rollout. | deferred |
| 9 | 2026-05-04 | M | Multi-arch image discipline | Phones are `arm64`, k3master/k3frigate are `amd64`. Any single-arch image silently fails to schedule on phones. | Build pipeline must produce multi-arch manifests (`docker buildx build --platform=linux/amd64,linux/arm64 ...`). Document as a CI rule once CI exists. | open |
| 10 | 2026-05-04 | L | GardenFlow not deployed | No `gardenflow` namespace in cluster. Local frontend points at `192.168.100.208:5000` which is one62 LAN IP -- currently broken given network split. | Plan GardenFlow deployment as a tutor-track milestone: containerize -> registry -> manifests -> namespace. Will exercise the whole pipeline. | open |
| 11 | 2026-05-04 | M | LoadBalancer stuck on `<pending>` | `default/guitar-backend` Service is type=LoadBalancer but EXTERNAL-IP shows `<pending>` (77d). Either MetalLB pool is exhausted/misconfigured for this Service, or it duplicates `guitar-frontend` which already has `.202`. | Inspect with `kubectl describe svc guitar-backend` + `kubectl -n metallb-system get ipaddresspools`. Either assign a pool, change to ClusterIP if internal-only, or delete if redundant. | open |
| 56 | 2026-05-11 | M | Traefik ingress controller installed but unused | `kube-system/traefik` is type=LoadBalancer holding `.200` (80/443), yet `kubectl get ingress -A` returns 0 resources. Every externally-exposed app (gitea, guitar-frontend, chickenflow-svc) bypasses Traefik via its own LoadBalancer IP. MetalLB pool drains for no reason. | Migrate apps behind Traefik with Ingress objects, free their dedicated IPs (`.202`, `.206`, `.211`). Keep `.200` as the cluster external entry. Pair with cert-manager later for TLS. | open |

---

## Closed items

---

## 2026-05-09 session findings (face-recognition + switch deploy + production polish)

| # | Date found | Sev | Topic | Finding | Proposed fix | Status |
|---|-----------|----:|-------|---------|--------------|--------|
| 12 | 2026-05-09 | H | Frigate MQTT silently broken cross-namespace | Frigate's `mqtt.host` pointed at `mosquitto.hydroflow.svc.cluster.local` (one6t pod IP). Cross-node Pod traffic from k3frigate to phone Pod IPs broken since 2026-04-29. Frigate published nothing for 10+ days; no log indicated failure. | Resolved: deployed local mosquitto in `frigate` ns pinned to k3frigate; repointed Frigate + Double Take. Underlying split-brain still tracked-elsewhere (#2). | done |
| 13 | 2026-05-09 | H | Frigate native face_recognition non-functional | Frigate 0.17 face_rec required exact frontal face capture. Sideways walks past cam17/cam22 = silent miss. Stack also failed because `/media/frigate/clips/faces/` was empty post-PVC migration to k3frigate. | Replaced with Double Take + CompreFace stack on k3frigate (CPU-only). Frigate native face_rec disabled globally + per-cam. | done |
| 14 | 2026-05-09 | M | k3master disk pressure | Root partition was 82% full (76 GB / 98 GB). k3s containerd cache held 23 GB of stale image layers. | Pruned via `crictl rmi --prune` — reclaimed 8 GB (now 73%). Recommend monthly cron prune. /home/user 18 GB still untouched (user review). | done (one-shot) |
| 15 | 2026-05-09 | L | Frigate /dev/shm too small for 1080p detect | Warning `SHM size of 256MB is too small, recommend ≥262MB` after bump to 1080p detect resolution on cam17 + cam22. | Bumped emptyDir sizeLimit 256Mi → 512Mi. | done |
| 16 | 2026-05-09 | M | Frigate OOMKilled after cam22 add | 5MP main stream + ArcFace large model + cam17 1080p detect saturated 3 Gi memory limit. | Bumped `resources.limits.memory` 3Gi → 5Gi initially, then trimmed to 4Gi after ArcFace strip + native face_rec disable. Steady ~3 Gi peak. | done |
| 17 | 2026-05-09 | M | CompreFace empty PVC shadowed bundled models | Mounting empty `compreface-models` PVC at `/app/ml/.models` hid the model files baked into the image. `facenet.Calculator` failed on `20180402-114759.pb not found`. | Removed PVC mount entirely; image-bundled models load fine. PVC + PV deleted. Pattern: only mount PVCs over data dirs, not over image-provided assets. | done |
| 18 | 2026-05-09 | L | CompreFace fe nginx env name + value format | `CLIENT_MAX_BODY_SIZE` env required (not `MAX_FILE_SIZE`); value must be nginx-compliant `10M` not `10MB`. Took 4 crashloops to land. | Documented in face_recognition.md. Required envs: `CLIENT_MAX_BODY_SIZE`, `PROXY_READ_TIMEOUT`, `PROXY_CONNECT_TIMEOUT`. | done |
| 19 | 2026-05-09 | M | CompreFace core OOMKilled at 2 Gi | TensorFlow + 7 plugins (face detector + landmarks + age + gender + mask + pose + calculator) saturate 2 Gi. | Bumped core mem limit 2Gi → 4Gi. Steady ~1.7 Gi after warmup. | done |
| 20 | 2026-05-09 | M | MetalLB Memberlist UDP probes failing | `Was able to connect to k3frigate over TCP but UDP probes failed, network may be misconfigured` — chronic warn flood between k3master ↔ k3frigate. Speaker still functional via TCP gossip. | Investigate WireGuard mesh UDP rules. Separate networking ticket. | open |
| 21 | 2026-05-09 | L | Jumphost dual-IP binding | Same MAC (`b8:70:f4:c6:60:70`) answers ARP on both `.62` (V17.2 official) AND `.152` (legacy). | Remove `.152` binding from jumphost iface (netplan or `/etc/network/interfaces`), restart networking. | open |
| 22 | 2026-05-09 | L | Suspicious locally-admin MACs on LAN | arp-scan showed devices at `.41/.51/.133/.140/.179` with locally-administered MAC bits set. | Inventory before VLAN segmentation — mostly likely chromecasts/IoT/random-MAC clients. | open |
| 23 | 2026-05-09 | M | cam22 NTP not syncing | Cam GUI hardening (NTP→`192.168.100.52`) didn't apply — embedded timestamp `03/05/2026` vs actual `09/05/2026`. | Re-set NTP via cam GUI using raw IP (DNS nulled per hardening). Verify drift. | open |
| 24 | 2026-05-09 | L | Liveness probes missing on face-rec stack | New deployments (compreface-{core,admin,api,fe}, postgres, double-take, mosquitto) had no liveness probe — stuck pods would not auto-recover. | Added TCP-socket liveness probes (initialDelay 60–180s, periodSeconds 30, failureThreshold 3) on all 7 deployments. | done |
| 25 | 2026-05-09 | L | CompreFace postgres lacked backup strategy | Training data + face library = irreplaceable; no dump strategy. | Deployed CronJob in `face-recognition` ns: daily 3am `pg_dump` + gzip to hostPath `/mnt/frigate/compreface/postgres-backups`, 14-day retention. Manually tested. | done |
| 26 | 2026-05-09 | L | Frigate cam15 chronic RTSP outage | `dial tcp 192.168.100.15:554: i/o timeout` for 20+ hours; cam likely offline (power/network). | Verify cam15 power/PoE/network. Not Frigate side. | open |
| 27 | 2026-05-09 | L | Frigate cam19_yicam codec parse fail | yi-hack stream had AAC audio + h264 video; ffmpeg couldn't lock codec params on sub-stream. | Switched cam19 to single main stream input with `-an` (drop audio) + bigger probesize. cam19 recording at 7 fps now. | done |
| 28 | 2026-05-09 | L | Frigate cam17 4K HEVC mislabeled | go2rtc URL `h264Preview_01_main` actually delivers H.265 because cam set to HEVC. Cosmetic — NVDEC handles both. | Update Frigate config comment for cam17 stream codec note. | open |


---

## 2026-05-09 follow-up (fix pass on open items)

| # | Update |
|---|--------|
| 3 | done — applied `homelab/role` labels: k3master=control-plane, k3frigate=video, one6t=mqtt, one62=db, one61=app. No workloads pinned via these yet (advisory). |
| 5 | reclassified — phone Adreno GPU is the intended target (arm64 image, OpenCL). Item was mistaken about k3frigate as target. Logs show last detection 2026-04-27 (12d stale) — pod alive but worker stuck. Separate ticket. |
| 6 | done — deleted errored pods (open-webui-...-98m7d, opencl-test, frigate-backup-test) + 3 failed Jobs. Frigate-backup CronJob has `successfulJobsHistoryLimit/failedJobsHistoryLimit` already; no recurrence expected. |
| 11 | partial — corrected pool annotation `default` -> `homelab-pool`. Real cause: shared-IP collision with guitar-frontend on .202. Runbook saved at `~/homelab-docs/lb-guitar-backend-fix.md` with 3 fix paths (ClusterIP / different IP / shared-IP annotation). User decision required. |
| 20 | partial — rotated MetalLB memberlist secret key (was version-88 mismatch causing decryption failures since k3s 1.34/1.35 skew). Speakers restarted clean. Residual UDP probe warnings remain due to phone-LAN split-brain (same root as #2) — TCP gossip works, MetalLB functional, treat as cosmetic until cross-node Pod-IP routing fixed. |
| 21 | runbook saved — `~/homelab-docs/jumphost-fix-runbook.md`. Sudo on .62 needs interactive pwd; user runs when ready. |
| 28 | done — Frigate config comment updated to clarify Reolink URL says `h264Preview` but delivers actual cam codec (H.265 in our config). |

| 21 | DONE 2026-05-10: applied sed to /etc/network/interfaces on jumphost (.62), removed live .152 binding via ip addr del. arp-scan confirms only .62 answers, .152 silent. |

| 11 | DONE 2026-05-10 (path A): converted guitar-backend Service to ClusterIP. Frontend reaches via in-cluster DNS (verified 404 from / = service alive). loadBalancerIP/.202 collision moot. |

| 23 | DONE 2026-05-10: cam22 NTP sync verified — embedded timestamp now  matches actual date. Time appears UTC (cam timezone may need Europe/Sofia adjustment for OSD readability — cosmetic). |

| 2 | DONE 2026-05-10: added 3 static routes on k3frigate (10.0.1.0/30, 10.0.2.0/30, 10.0.3.0/30 via 192.168.100.52). Persisted in /etc/netplan/50-cloud-init.yaml. VXLAN flow restored: k3frigate -> phone Pod CIDRs (10.42.{1,2,3}.x) all alive. Verified mosquitto.hydroflow:1883, gitea.default:80, postgres.hydroflow:5432 all reachable from frigate-namespace pods. |
| 12 | Updated 2026-05-10: underlying split-brain (#2) now fixed too. Local mosquitto in frigate ns still in place — redundant but kept for isolation; revisit if cleanup desired. |

| 29 | DONE 2026-05-10: hydroflow postgres backup CronJob deployed (hydroflow ns). pg_dump 03:30 daily -> scp to user@192.168.100.62:/home/user/backups/hydroflow-postgres/. 14-day retention. Reuses jumphost-ssh-key secret (copied from frigate ns). Manually tested: 3.8K dump landed on jumphost. |
| 30 | DONE 2026-05-10: hydroflow postgres migrated from one62 (phone eMMC, 5Gi local-path) to k3frigate (i5-6600 SATA, 10Gi hostPath /mnt/frigate/hydroflow-postgres). Live data piped via kubectl exec pg_dump | psql. 7 tables verified identical row counts. Service selector swapped to instance=new. hydroflow-backend + chickenflow-backend restarted, reconnected. Old deploy scaled to 0 (kept 48h for rollback). Phase 2 (intermediate target = k3frigate; SP4 future home). |
| 31 | DONE 2026-05-10: frigate-backup CronJob target IP corrected .152 -> .62 (was failing silently since jumphost moved). |

| 12 | Final 2026-05-10: dedicated Frigate broker is intentional (kept by design). Reasons: low-latency video event processing, isolation from hydroflow phone broker, blast-radius separation. Configuration locked in as "two brokers, two domains" — frigate ns owns Frigate+DT events, hydroflow ns owns sensor+backend traffic. |
| 32 | DONE 2026-05-10: full cluster k3s upgrade v1.34.3+k3s3 -> v1.35.0+k3s3 (k3master + k3frigate). Procedure: backup state.db + /etc/rancher/k3s + manifests off-host -> cordon -> curl get.k3s.io \| INSTALL_K3S_VERSION=v1.35.0+k3s3 sh - (agent needs K3S_URL+K3S_TOKEN) -> verify -> uncordon. Pre-upgrade backups in /home/ivalin/k3s-backups/. Lesson: exclude var/lib/rancher/k3s/agent/containerd + agent/images from agent backups (containerd image cache ~1.5GB, not needed). |
| 33 | DONE 2026-05-10: chickenflow-backend root-cause fixed (was crashlooping 18d). Cause: chickenflow-db-secret pointed at hydroflow postgres NodePort but chickenflow_user / chickenflow db never provisioned (verified absent in BOTH old + new hydroflow postgres). Resolution: dedicated chickenflow-postgres deploy in chickenflow ns on k3frigate (postgres:15-alpine, 2Gi PV /mnt/frigate/chickenflow-postgres, ClusterIP svc). Secret URL switched to ClusterIP DNS chickenflow-postgres.chickenflow.svc:5432/chickenflow. Backend up, /api/health dbWritable=true. Backup CronJob mirrors hydroflow pattern, schedule 04:00 daily, dropped nodeSelector (k3frigate at 99% memory requests, backup runs anywhere via cluster DNS). Tested: 3.5K dump on jumphost. |
| 34 | OBS 2026-05-10: k3frigate memory pressure 99% requests (15942Mi/16Gi). Limits 136%. Workloads: postgres-new (hydroflow), chickenflow-postgres, frigate, double-take, mosquitto, compreface stack, nvidia-device-plugin. New pods with mem request fail to schedule there. Either (a) trim requests on existing workloads (compreface oversized?), (b) add another node, (c) live with cluster-wide scheduling for non-pinned jobs. Tracked: open. |
| 35 | DONE 2026-05-10: row 34 partially addressed. Patched StatefulSet loki-chunks-cache (default ns, helm release loki-6.53.0): memcached -m 8192 -> -m 256, requests/limits 9830Mi -> 320Mi. k3frigate memory pressure dropped 99%->38% requests, 136%->74% limits (~9.6Gi freed). Pod restarted, rescheduled to one62 (no affinity pin). **Helm-managed: next `helm upgrade loki` will revert.** Permanent fix = update values: `chunksCache.allocatedMemory: 256`. Justification: actual memcached usage was 0Mi; homelab loki traffic ~5 daemonset canaries, default chart sized for prod scale. results-cache (-m 1024 / 1229Mi) left untouched. |
| 36 | DONE 2026-05-10: row 35 made permanent. Created ~/homelab-config/monitoring/loki-values.yaml capturing all user-supplied loki helm values + chunksCache.allocatedMemory: 256 override. Ran helm upgrade loki grafana/loki -n default --version 6.53.0 -f loki-values.yaml. Chart now renders -m 256 + 307Mi req/lim natively (no longer drift on next upgrade). Committed locally to homelab-config (47c3255), NOT pushed to github/origin pending user review. |
| 37 | DONE 2026-05-10: compreface-fe exposed via MetalLB LoadBalancer 192.168.100.212 (was ClusterIP, no LAN access). Manifest patched in ~/homelab-config/apps/face-recognition/23-compreface-fe.yaml (commit 595b4bf, pushed github + gitea). Other compreface-* + postgres stay ClusterIP (admin/api proxied internally by fe). UI live: http://192.168.100.212/ for face DB management (apps, subjects, image upload, API keys for Double Take). |
| 38 | DONE 2026-05-10: cam21 (ANNKE C500, .21) added to Frigate config — same hardware as cam22 (Attic). Mirrored cam22 blocks (go2rtc + cameras) in /mnt/frigate/config/config.yml on k3frigate, restarted deploy/frigate, manually synced ConfigMap. cam14 stays disabled (replaced by cam21 in monitoring coverage). cam23/.24 pending same swap. IPcams.md updated. NOTE: ~/frigate-k8s/configmap.yaml in repo still stale (cam22+cam21 absent there) — has unrelated dirty changes, refresh deferred. |
| 39 | OBS 2026-05-10: Frigate config-sync sidecar has init bug. Logic: `LAST_HASH=""` on container start, first poll computes CURRENT_HASH but sync gated by `if [ -n "$LAST_HASH" ]` so first iteration sets LAST_HASH=current, no sync. Subsequent edits sync correctly. Bug surfaces when: (1) hostPath edited externally before sidecar restarts (cam22 case), (2) Frigate rollout-restart resets LAST_HASH and any pending hostPath delta is silently swallowed. Fix: initialize LAST_HASH from current ConfigMap hash on startup, or compare CURRENT_HASH vs cm hash directly each poll. Tracked: open. |
| 40 | DONE 2026-05-10: row 39 fixed. Frigate config-sync sidecar rewritten to compare hostPath md5 vs ConfigMap md5 each iteration (no in-memory LAST_HASH). RBAC already allows `get` on `frigate-config` cm, no change needed. Bumped sidecar memory 64Mi -> 192Mi (was OOMKilling on `kubectl create cm --from-file=10K-config | apply`, exit 137). Validated: edit hostPath, sync within 5-6s. Reverse edit also synced. NOTE: deploy patched in cluster, ~/frigate-k8s/deployment.yaml in git is stale (already had unrelated dirty changes, refresh deferred). |
| 41 | OBS 2026-05-10: Frigate UI URL is http://192.168.100.209:5000/ (port **5000**, not 80). Service exposes 5000 (web), 8554 (rtsp), 8555 (webrtc-tcp). Update bookmarks/docs that reference port 80. |
| 42 | DONE 2026-05-10: cam21 face recognition wired via Double Take + CompreFace path. Steps: (a) added cam21 to double-take-config under cameras.cam21.snapshot.attempts=5 (mirrors cam22), (b) toggled Frigate native cam21.face_recognition.enabled true→false to avoid duplicate pipeline, (c) restarted DT + Frigate. cam21 now flows: Frigate person event → MQTT frigate/events → DT → CompreFace k3face app. Match destination /mnt/frigate/double-take/matches/<subject>/. Pre-req before matches appear: enroll reference photos per subject in CompreFace UI (5 subjects exist with 0 images, library purged 2026-05-09). |
| 43 | OBS 2026-05-10: cam17_reolink stream is severely overexposed (frame nearly uniform white). Detector runs at 25fps but classifies almost nothing as person — only 2 of last 200 events vs cam19/cam22 with 92/72 each. Root cause = camera HW exposure setting, not Frigate config. Fix at Reolink web UI 192.168.100.17 (Brightness/WDR/Backlight Compensation/Exposure mode). Tracked: open. |
| 44 | OBS 2026-05-10: cam22 (ANNKE .22) network DOWN (ping fails, ffmpeg gets RTSP timeout dial tcp 192.168.100.22:554). Camera was deployed off bench earlier today, may be PoE/Wi-Fi/cable issue. No detections possible until reachable. Tracked: open. |
| 45 | DONE 2026-05-10: double-take UI exposed via MetalLB 192.168.100.213 (was ClusterIP). Manifest patched in ~/homelab-config/apps/double-take/20-deploy.yaml. UI live: http://192.168.100.213:3000/ for unknown crop review + label-to-subject workflow (passive auto-enrollment path). DT unknown.purge bumped 8h -> 168h (7d) so users have time to review unknowns before deletion. |
| 46 | UPDATE row 43: cam17 NOT overexposed per user — frame is bright but gate/path visible, low event count is area traffic not HW. Retracted overexposed tag in IPcams.md. cam17 events: 2 of last 200, attribute to no-one-walking-by rather than detector failure. |
| 47 | DONE 2026-05-10: double-take latent bugs surfaced + fixed when Reloader rolled pod after our cm edits. (a) EROFS: cm mounted at /.storage/config readOnly blocked DT writing secrets.yml — switched to subPath: config.yml so only the file is overlaid, dir stays writable from PVC. (b) Readiness probe httpGet /api/status returned 404 in DT v1.13.11.8 (endpoint removed) — switched to /. Pod was running 1/1 only because previous container layers had secrets.yml cached from old layout; first cold start after Reloader exposed both bugs. Manifest updated in ~/homelab-config/apps/double-take/20-deploy.yaml (commit f0c4152). Lesson: always cold-restart deploys after cm/secret edits to flush latent layout bugs early. |
| 48 | DONE 2026-05-10: DT MQTT broker switched mosquitto.hydroflow -> mosquitto.frigate.svc.cluster.local. Root cause of 0 cam17/cam21 matches: Frigate publishes events to local frigate-ns mosquitto since 2026-05-09 (cross-node MQTT to one6t hydroflow broker broken), but DT was still subscribed to hydroflow broker — frigate/events never reached it post-restart. Fix: align DT to same broker. Manifest committed (2d4ed1f). cam17/21/22 should accumulate matches normally now once persons walk past. |
| 49 | DONE 2026-05-10: added frigate-family CLI on .52:~/bin/ + laptop ~/bin/. Lists Frigate events with sub_label IN (Goran,Ivalin,Miraya,Rayna,Virginia). Counterpart to frigate-strangers. Frigate UI bookmark URL for family: http://192.168.100.209:5000/events?sub_labels=Goran,Ivalin,Miraya,Rayna,Virginia (UI doesn't support negation, so strangers stays CLI-only). IPcams.md updated. |
| 50 | OBS 2026-05-10: DT processing events post-broker-fix but CompreFace returns 0 faces on snapshots (counts: person:0, match:0, miss:0, unknown:0). Cause = snapshot quality — Yi cam 640x480 sub stream + motion blur defeats CompreFace face detection. Also enrollment uploads showed many "No face is found" / "More than one face" errors during DT training. Two fronts to improve: (a) feed CompreFace higher-res snapshots (DT image.height in config could go to 1080), (b) enroll only single-face well-lit reference photos. Sort CLI/URL still works once matches start landing. Tracked: open. |
| 51 | DONE 2026-05-10: DT face_rec scope reduced. Per-cam labels allowlist with global frigate.labels=[]. Only cam17_reolink + cam21 process events. Reason: low-res cams (XMeye, Yi) produced CompreFace mismatches/no-detect on faces that human eye can read clearly — pixel count too low after DT image-resize. cam22/cam23/cam24 (ANNKE) and cam18 (Reolink) reserved for future ON when deployed; currently OFF. cam22 specifically dropped from cameras list (was in scope, no longer). homelab-config commit 621d83c. |
| 52 | DONE 2026-05-11: hybrid-AI workflow (local Ollama on .50, qwen2.5-coder:14b on RTX 3060 12GB) DEPRECATED. Real-world tests 2026-05-10: PowerShell VRAM-cap script (3 attempts: typo, spec drift, scope mismatch — none usable raw) + bash kubectl audit (2 logic bugs, 100% noise output). Cloud rewrite faster than LAI fix-loop. Privacy use cases didn't materialize for homelab work. Default routing now Opus 4.7 + Sonnet 4.6 sub-agents via Agent tool (subagent_type Explore for read-only / general-purpose for write / Plan for design). .50 stays asleep, no auto-cron. Skill ~/.claude/skills/hybrid-ai/SKILL.md rewritten as deprecation notice + revival pointer. Surviving artifacts: ~/bin/ollama-tune-vram-cap.ps1 (laptop), ~/hybrid-ai-test/ (laptop), github.com/ivemcfire/hybrid-ai-k3s. |
| 53 | DONE 2026-05-11: hardware change on .50 — RTX 3060 12GB removed + Ollama uninstalled. Replaced with GTX 1050Ti (4GB) for Windows daily-driver use. Local AI on .50 now permanently offline (4GB insufficient for qwen2.5-coder:14b Q4_K_M which needs ~9GB). Endpoint 192.168.100.50:11500 will not respond. WoL still works for waking the desktop. Skill SKILL.md, MEMORY.md, reference_hybrid_ai*.md, hybrid-ai-k3s repo README updated to reflect hardware-removed state. 3060 destination TBD — could land on k3frigate (would replace 1050Ti = major Frigate detector + face_rec speedup, ~10x inference) or another future host. |
| 54 | DECIDED 2026-05-11: RTX 3060 destination = son's gaming PC, **out of homelab entirely**. k3frigate stays on GTX 1050Ti. No GPU-tier upgrade planned for cluster. Closes row 53 speculation. Frigate detector + CompreFace stay on current hardware (16ms inference, CPU face-rec). |
| 55 | DONE 2026-05-11: silenced charger plug/unplug "puck" sound on all 3 phone workers (one6t/one61/one62). Root cause = libcanberra playing /usr/share/sounds/freedesktop/stereo/power-{plug,unplug}.oga on UPower events (NOT feedbackd — false lead). 70%-capped USB hub PD makes phones cycle charger often, amplifying the noise. Fix per-user (no root, doas needs password): `gsettings set org.gnome.desktop.sound event-sounds false; gsettings set org.sigxcpu.feedbackd profile silent` via `DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/10000/bus`. Baseline was inconsistent (one61 already had fbd=silent, others fbd=full), now uniform. Reversible via `gsettings reset`. Surprise finding: phones run **systemd + phosh on tty1** (not OpenRC headless as repeatedly assumed). Documented at ~/.claude/skills/k8s-ops/references/phone-os.md + linked in library.md. Read that before any phone work. |
| 56 | DONE 2026-05-12: IPcams.md inventory refresh + ANNKE ONVIF assessment. Active set confirmed: Xiongmai .11/.12/.13/.15/.16 (cam14 RETIRED, replaced 2026-05-10), Reolink 4K .17/.18 (cam18 reassigned from Xiongmai slot — same model as cam17), ANNKE C500 .21/.22/.23/.24, Yi .19. New device-spec block + creds block for ANNKE. ONVIF status on ANNKE = **enabled on cam UI** (ports 8000 default, RTSP 554). **Decision: do NOT wire cam-side ONVIF analytics (motion/line-cross/intrusion) nor ONVIF event subscription to HA/Frigate/MQTT.** Reasoning: ANNKE C500 = entry-level detector logic, high FP rate, would degrade alert reliability vs Frigate's object detector. Architectural bloat for zero signal gain. ONVIF probing (onvif-cli/wsdd) skipped — RTSP feed sufficient unless connectivity troubleshoot needed. |
| 57 | OBS 2026-05-12: ANNKE creds question resolved — shared with Reolink. Frigate cm uses single env-var `FRIGATE_REOLINK_PASSWORD` (k8s secret `frigate-rtsp-creds` in `frigate` ns) interpolated into RTSP URLs for cam17/18 (Reolink) AND cam21/22 (ANNKE). Same admin password on all 4. Var name = misnomer post-ANNKE addition, rename to `FRIGATE_RTSP_PASSWORD` on next refactor (low-priority cleanup). Also: `~/frigate-k8s/configmap.yaml` in git is **stale** (missing cam21/22 sections + face_recognition.enabled differs from in-cluster cm). Pairs with stale `deployment.yaml` flagged row 40. Both should be refreshed-from-cluster + committed in one pass. Tracked: open. |
| 58 | DONE 2026-05-12: **Phase 2 HA migration complete.** k3master (.52) rerolled from sqlite single-master to embedded etcd via `k3s install --cluster-init`, k3frigate (.56) and k3SP4 (.53) joined as control-plane peers. 3-CP etcd quorum live (k3master + k3frigate + k3sp4 all control-plane,etcd). Phones (one6t/one61/one62) re-onboarded as agents via USB peer IPs (10.0.X.1, --flannel-iface usb0). 6 nodes Ready total. Workloads restored via stripped-yaml + helm reinstall + IaC apply from ~/homelab-config/. Outage: ~55 min wall time including PVC binding fixes + CRD recovery. **Issues surfaced during reroll:** (a) backup CRD yaml had `tag:yaml.org,2002:value` constructor that broke python yaml.safe_load_all — MetalLB v0.14.9 + Prometheus operator v0.89.0 CRDs reinstalled from upstream bundles, all 50 CRDs restored. (b) Static PVs from backup had stale claimRef UIDs pointing to old PVCs — patched via `kubectl patch pv ... --type=json -p='[{"op":"remove","path":"/spec/claimRef"}]'` for chickenflow-postgres-pv, compreface-*, double-take-pv, hydroflow-postgres-new-pv. (c) nvidia-device-plugin-daemonset DESIRED=0 — k3frigate needed `nvidia.com/gpu.present=true` label added back. (d) IaC gap fillers committed to ~/homelab-config/apps/ (commit 8465d89) for chickenflow, hydroflow-backend, hydroflow-mosquitto (re-pinned to k3frigate, hostPath /mnt/frigate/mosquitto), gpu-inference, smarter-device-manager. (e) Phone rejoin script v1 had quoting bug eating env vars — v3 fix uses staged /tmp/k3s-rejoin.sh + single doas exec per phone. **Open follow-ups (NOT blocking):** (1) ~/frigate-k8s/ git dirty (3 modified, 1 untracked) — flagged for review/commit. (2) Loki + monitoring stack NOT in `helm list` post-reroll (only traefik, traefik-crd, reloader) — raw yaml restored, helm release metadata lost; future helm upgrades will fail until reinstalled with helm install --replace. (3) guitar-backend/frontend + mqtt-inference-worker on ErrImageNeverPull — phone-local images need rebuild or nodeSelector pin to correct phone. (4) script bug fixed in cluster: k3frigate-server-reroll.sh had FLANNEL_IFACE=wan0 hardcoded, should be enp2s0 for .56 — patched in script. |
| 59 | DONE 2026-05-13: jumphost migration .152 → .62 doc cleanup pass (user flagged docs lagged behind move from a week ago). Updated active-state references: `RECOVERY.md` (4 occurrences — age key paths, scp examples, jumphost IP table row), `info.md` (backup script comment + path), `~/backup-to-jumpbox.sh` (line 19 scp target — **backup pipeline was silently broken since migration**, .52 → .152 cron failing nightly). Left historical: `blog-frigate-nvr.md` (published 2026-04, frozen narrative), `cluster-improvements.md` history rows referencing .152 at the time of event, `jumphost-fix-runbook.md` (the migration runbook itself, describes the change). `library.md` was already current with .62. **Open follow-ups:** (1) verify `~/frigate-k8s/backup-cronjob.yaml` + in-cluster frigate-backup CronJob don't have stale .152 (checked at time of this row — find/update if so). (2) jumphost .62 has docker 29.2.1 + buildx linux/amd64 only — needs multi-arch (qemu + arm64) builder set up before next image rebuild push to gitea (gitea-as-registry recovery also pending per row to-be-added). |
| 60 | DONE 2026-05-13: build infra moved off qemu-on-.62 to **buildx kubernetes driver**. Cluster pods do the heavy lifting on native arch: `builder-amd64` deployment on k3sp4, `builder-arm64` on one62 (auto-scheduled by nodeselector). RBAC + namespace + ConfigMap at `~/homelab-config/apps/buildkit/`. Builder created from .52 via `docker buildx create --driver=kubernetes --bootstrap --config /tmp/buildkitd.toml`. **Key gotchas surfaced:** (a) buildx serializes `--config` to its auto-generated CM with single-quoted TOML registry keys (`[registry.'192.168.100.206']`) but buildkit's resolver only honors **double-quoted** keys for IP-only hosts — fix: `kubectl patch cm builder-{amd,arm}64-config` post-create with proper TOML, then `kubectl rollout restart`. (b) Even with correct config, buildkit's resolver does NOT apply `http=true` to IP-only registry hosts (192.168.100.206 still hits 443). Workaround: push via cluster DNS hostname `gitea.gitea.svc.cluster.local` (added `/etc/hosts` entry on .52 mapping to .206, added that hostname to docker daemon.json insecure-registries and to `~/.docker/config.json` auths). (c) Multi-arch `--push` merge step runs client-side via docker CLI which can't bypass HTTPS — manifest-list assembly across both archs failed. Single-arch builds (per-arch tags) push fine. For hydroflow-backend = arm64-only (all worker nodes are phones / arm64), so single-arch is sufficient. **Phase B/C results:** `homelab/gpu-worker-onnx:20260513-0543` (arm64, CPU EP — onnxruntime-opencl has no arm64 wheel, used Dockerfile.onnx-cpu which matches what was running in cluster pre-reroll), `homelab/mqtt-inference-worker:20260513-0544` (arm64), `ivemcfire/hydroflow-backend:20260513-0554` (arm64). All present in gitea registry, anonymous-pull denied, pull secret `gitea-registry` recreated in hydroflow + gpu-inference ns with ci-user creds. **Created gitea org `ivemcfire`** (via admin API) and added ci-user to Owners team so it can push to that namespace. **Build times (native vs prior qemu):** gpu-worker-onnx arm64 base 1m22s (cached 4s), mqtt-worker arm64 1m13s, hydroflow-backend arm64 33s. Prior qemu approach on .62 timed out / aborted on Dockerfile.onnx step alone (`pip install onnxruntime-opencl` package non-existent — same blocker as native; switching arch via qemu doesn't change PyPI availability). **Phase D done:** open-webui (deploy+svc) deleted from default ns + IaC; default-ns `ollama` ClusterIP svc deleted (chickenflow-ns ollama kept — different svc); loki helm-installed into monitoring ns from `~/homelab-config/apps/monitoring/loki/values.yaml` (chart 6.53.0, lokiCanary explicitly disabled — chart key moved out of `monitoring.lokiCanary`); old default-ns loki sts×3 + ds/loki-canary + svcs×6 + cms + sa + PVC deleted; uptime-kuma re-applied to monitoring ns at `~/homelab-config/apps/monitoring/uptime-kuma.yaml` (LB 192.168.100.205 reused, emptyDir kept); guitar fluent-bit sidecar cm + IaC also removed (orphan from guitar deletion). `kubectl get all -n default` = only `service/kubernetes`. **Open follow-ups (BLOCKING for mqtt-inference + hydroflow pods to run):** (1) Phones (one61/62/6t) have **no `/etc/rancher/k3s/registries.yaml`** — kubelet pulls from 192.168.100.206 hit 443 (no route). doas needs interactive password so couldn't write the file from .52. User must, on each phone: `echo 'mirrors:\n  "192.168.100.206":\n    endpoint:\n      - "http://192.168.100.206"\nconfigs:\n  "192.168.100.206":\n    tls:\n      insecure_skip_verify: true' | doas tee /etc/rancher/k3s/registries.yaml && doas mkdir -p /etc/rancher/k3s && doas rc-service k3s-agent restart` (postmarketOS uses OpenRC; if systemd: `doas systemctl restart k3s-agent`). Until then, mqtt-inference-worker + hydroflow-backend pods remain ImagePullBackOff. (2) hydroflow-backend lost amd64 build — accept arm64-only or fix client-side insecure-HTTPS workaround for buildx imagetools (e.g. run `crane` from inside cluster pod, or use buildkit pod's `buildctl` directly). (3) chickenflow/cm-config.yaml references `http://ollama.chickenflow.svc.cluster.local:11434` — chickenflow-ns ollama svc EXISTS so this is live; flagged per brief, no action. (4) Buildkit pod on one62 (arm64 builder) is heavy (~2-3Gi during build) — phone has 4Gi RAM. If memory pressure during simultaneous loki/uptime-kuma activity, pin builder to one6t (8Gi) via additional nodeSelector. (5) `Dockerfile.worker` was edited to FROM gitea-registry rather than node-local — `~/gpu-worker/Dockerfile.worker.bak` preserves original. |
| 61 | DONE 2026-05-13: phase 2 HA migration **fully green**. Three lingering blockers from row 60 closed: (a) **k3s 1.35 hosts.toml synthesis bug** — k3s reads `endpoint: ["http://192.168.100.206"]` from registries.yaml but writes `server = "https://192.168.100.206/v2"` in `/var/lib/rancher/k3s/agent/etc/containerd/certs.d/<host>/hosts.toml`. Containerd then silently ignores the `[host."http://..."]` mirror block, all pulls go to :443 → "no route to host" (gitea only serves :80). Worth filing upstream. Fix: install `/usr/local/bin/fix-gitea-hosts.sh` on all 6 nodes that overwrites hosts.toml with correct http-only entry; persist via systemd drop-in `/etc/systemd/system/k3s{-agent,}.service.d/10-gitea-hosts.conf` ExecStartPost. Applied on phones first, then preemptively on .52/.56/.53 (CPs were latently broken too — no CP-scheduled pod pulls .206 so problem was hidden). .53 fix delivered via `kubectl debug node/k3sp4` chroot since SSH user is `ivalin` not `user`. (b) **hydroflow-backend probe** — `/health` returned 404, real endpoints `/healthz` (200) + `/readyz` (503 when DB unreachable). Updated probes to `/healthz` for both liveness + readiness in `~/homelab-config/apps/hydroflow-backend/deploy.yaml`. (c) **hydroflow-backend DB env missing** — pod connected to `127.0.0.1:5432` (localhost) and failed. Added explicit env mapping: `DB_HOST=postgres.hydroflow.svc.cluster.local` (literal), `DB_PORT=5432` (literal), `DB_USER/PASSWORD/NAME` ← `postgres-secret` keys `POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB` (backend `~/hydroflow/backend/db.js` uses `DB_*`, secret uses `POSTGRES_*` — explicit mapping not `envFrom`). `/readyz` now 200 `{"status":"ready","db":"connected"}`. **Phone NOPASSWD doas** set up earlier (matches .52/SP4 pattern) so subsequent sub-agents drive phones autonomously — see `feedback_phone_scripts.md` on laptop memory. **Final state:** 6 nodes Ready, all previously-broken pods Running, default ns empty (only `service/kubernetes`), gitea registry serving images correctly to all 6 nodes, CPs preemptively patched. 4 commits pushed to github/main: `8465d89 9d9782d 1afacfc c45da70`. Gitea `origin` remote push failed (no creds for .206) — github is authoritative, gitea remote 4 commits behind, low priority unless ArgoCD-style reader added later. |

---

## 2026-05-27 — Multi-fault outage during jumphost-hardening session

**Intent:** Convert jumphost (.62) into a real production bastion — 6-phase plan (sshd hardening → admin tooling → ProxyJump → source-control → backup → IP cleanup).

**What completed cleanly:**

- **Phase 1: sshd hardening on .62** — drop-in at `/etc/ssh/sshd_config.d/10-hardening.conf` (PasswordAuth no, RootLogin no, MaxAuthTries 3, modern KexAlgorithms/Ciphers/MACs, ClientAliveInterval 300). fail2ban installed with `sshd-bastion.local` jail (10m findtime / 1h bantime / ignoreip 192.168.100.0/24 + 100.64.0.0/10). Safety-revert systemd timer pattern proven (5-min auto-rollback if locked out).
- **Phase 2: Admin toolkit on .62** — `kubectl v1.35.1` + `yq v4.45.1` + `jq` installed. kubeconfig at `~/.kube/config` pointing at `https://192.168.100.52:6443`. Will smoke-test against cluster once it recovers.

**What broke during Phase 3 (the cascade):**

Three independent failures stacked and obscured each other for hours:

1. **.52 wan0 USB ethernet (r8152 driver) carrier-flapping**
   - dmesg shows repeated `r8152 4-5.3:1.0 wan0: carrier off / carrier on` events
   - Every flap kills in-flight TCP — including etcd raft peer probes, kubectl
   - User physically swapped the original CDC NCM adapter for a Realtek r8152 mid-session (improvement, but new adapter still flapping)
   - **TODO:** swap for a PCIe NIC or a different USB3 adapter that doesn't flap

2. **Original ZyXEL GS1200-8 v3 service-room switch failed permanently**
   - MAC `c0:2e:5f:57:fe:d4`. Became unresponsive (no ICMP, no HTTPS UI, no LLDP) during a routine password-recovery factory reset. Two 15-second resets + power-cycle did not bring it back.
   - **Hardware presumed dead.** Replaced in service-room rack by the "Inverter cabinet" spare unit, now configured to `192.168.100.3` and serving cluster L2.
   - Sticker-default passwords now recorded in `credentials.md.age` (commit `5aa1416`, GitHub push pending DNS) along with new schema (location/IP/MAC/sticker-pwd/current-pwd/SNMP-community).
   - **DevOps lesson added to cred doc:** photograph every device label at unboxing, record sticker pwds before rotating, store images under `~/homelab-secrets/photos/` (gitignored).

3. **Tailscale `--accept-routes=true` hijacked LAN traffic on every Tailscale-enabled node**
   - With `.52` and `.62` advertising `192.168.100.0/24` as subnet routers, every other Tailscale peer (including the laptop) installed a policy rule sending `192.168.100.0/24 → table 52 → dev tailscale0`
   - This *replaced* the direct wifi/LAN route to cluster IPs with a Tailscale tunnel — which would silently fall back to DERP relay when peers were "online" on Tailscale but had bad LAN
   - **This masked every LAN diagnosis for ~4 hours** — we thought switch ports were partitioned when in fact the laptop was just routing all traffic through degraded Tailscale tunnels
   - **Fix:** `sudo tailscale set --accept-routes=false` on the laptop, .52 (done by us). User to run same on **.53, .56, .62** at next session.
   - Subnet routers should generally NOT accept their own advertised subnet — creates a routing self-loop. Add this to the [[tailscale_mesh]] memory.

**Final state at session end:**

| Node | LAN | Tailscale | Comment |
|---|---|---|---|
| Switch `.3` (formerly Inverter spare) | ✓ | n/a | No config — works on factory defaults. Set static IP + Save Config next session. |
| `.52` k3master | flapping | active | r8152 USB ethernet still unstable. ICMP intermittent, TCP unreliable. |
| `.53` k3sp4 | sometimes | active | etcd LISTEN on `192.168.100.53:2380` confirmed. Reachable in some windows, not others. |
| `.56` k3frigate | sometimes | active | etcd LISTEN on `192.168.100.56:2380` confirmed. Same pattern as .53. |
| `.62` jumphost | ✓ | active | Stable. Phases 1+2 of hardening completed. |
| k3s cluster | DOWN | n/a | etcd no quorum; apiserver `ServiceUnavailable` 503; `kubectl get nodes` times out from .62 and locally on .52. |

**Pending commits stuck locally** (GitHub push blocked by DNS resolution failure on .52 — clear when cluster network stabilizes):
- `homelab-docs@15c6525` — initial sticker pwd capture
- `homelab-docs@5aa1416` — full sticker schema + post-incident topology

**Resume plan for next session (priority order):**

1. **Verify network is now stable** — ping matrix from laptop + jumphost to all CP nodes. If still flapping, replace `.52`'s USB ethernet adapter first (or check switch port 8 link stats).
2. **User runs `sudo tailscale set --accept-routes=false` on .53, .56, .62** — finish the Tailscale subnet hijack cleanup.
3. **Restart k3s on all 3 CP nodes** (`systemctl restart k3s`) once network is stable for ~5 min. Watch etcd raft re-establish.
4. **Smoke-test:** `kubectl get nodes` from `.62`, expect 6 Ready. `cloudflared` HA pods should also recover (we set this up earlier this week).
5. **Retry pending GitHub pushes** from `.52` once DNS resolves.
6. **Configure new `.3` switch properly:** set static IP `192.168.100.3` (or back to `.2` if you prefer), set SNMP community, change admin pwd off sticker default, **Save Config explicitly** (GS1200 quirk), record new pwd to `credentials.md`.
7. **Resume Phase 3 of jumphost hardening** (ProxyJump pattern + AllowUsers lockdown on k3 nodes).
8. **Phases 4-6** to complete the original plan.

**Open issues to file:**
- `.52` USB ethernet (r8152) carrier flap — needs hardware fix (different adapter or built-in NIC) before this node is reliable
- Original service-switch hardware failure — three GS1200-8 v3 units now in the rack history, one dead, plan to evaluate vendor reliability
- DNS resolution from `.52` flaky during outage — investigate resolver config / consider local Unbound

| 62 | **Frigate storm-load hardening (2026-05-27).** During the 2026-05-27 rainstorm, all 8 cameras triggered motion detection + recording simultaneously. Frigate pod had `requests.cpu: 1` but **no CPU limit**. ffmpeg + ONNX inference goroutines consumed all 4 cores of i5-6600K (4C, no HT), starving kubelet + etcd heartbeats. Node fully locked up twice in 48h (k3s cold-restarted at 2026-05-25 08:59 and 2026-05-27 10:06). No kernel OOM lines (sufficient 16GB + no swap), CPU starvation the culprit. **Hardening:** (a) `~/frigate-k8s/deployment.yaml` resources: `limits.cpu: "3"` (was unset — largest win; leaves 1 core for OS+k3s), `requests.memory: 4Gi` (was 1Gi), `limits.memory: 6Gi` (was 3Gi), `shm sizeLimit: 512Mi` (was 256Mi). (b) k3frigate kubelet `/etc/rancher/k3s/config.yaml` added: `kubelet-arg: ["kube-reserved=cpu=500m,memory=512Mi","system-reserved=cpu=250m,memory=256Mi","eviction-hard=memory.available<200Mi,nodefs.available<10%"]`, then `sudo systemctl restart k3s`. (c) `/etc/default/zramswap`: `ALGO=zstd`, `PERCENT=25` (4GB on 16GB), `PRIORITY=100`, `sudo systemctl enable --now zramswap.service` → 3.9G `/dev/zram0` priority 100. (d) Frigate `/mnt/frigate/config/config.yml`: cam21/22 `detect.width/height: 1280x720` → `640x360` (ONNX model is 320x320, higher res = wasted CPU); cam19_yicam stream split into separate `cam19_sub` (detect) + `cam19` (record), was forcing decode+reencode cycle. **Note:** hostPath `/mnt/frigate/config/config.yml` is live source-of-truth; ConfigMap `frigate-config` is downstream backup synced by sidecar. git-tracked `~/frigate-k8s/configmap.yaml` is stale (missing cam21/22, face_recognition.enabled differs). **Not yet done:** tighter motion masks on cam21/22/19 (wind-shaken trees during storms = constant detect); `face_recognition.model_size: large` → `small` if VRAM tight under load; nvidia-device-plugin init/readinessGate to prevent scheduling pre-plugin-recovery (caused `UnexpectedAdmissionError` during 2026-05-27 recovery). |
| 63 | **coredns external DNS fixed (2026-05-27).** After Tailscale install on 2026-05-26, k3master (.52) networkd-resolved hook left `/etc/systemd/resolved.conf` empty. coredns Corefile had `forward . /etc/resolv.conf`, which on k3s still points to `127.0.0.53` (systemd-resolved stub). **That stub is NOT reachable from inside the coredns pod netns.** Result: all pods failed external DNS (`pip install`, image pulls, cloudflared tunnel registration hang). Symptom: `getent hosts cloudflare.com` from inside a pod = `Try again` (resolver-level timeout, no upstream answered). **Fix:** Edit the `coredns` ConfigMap in `kube-system` (`kubectl edit cm coredns -n kube-system`) to replace `forward . /etc/resolv.conf` with `forward . 1.1.1.1 8.8.8.8`. Then `kubectl rollout restart -n kube-system deployment coredns` to reload. Verify: `kubectl exec -it -n default <any-pod> -- getent hosts cloudflare.com` = instant resolve. **Root cause recap:** systemd-resolved stub at 127.0.0.53 is host-local, not pod-accessible; hardcoded upstream IPs bypass the host's resolver config but actually work. See row 64 for the companion host-level fix (restored /etc/systemd/resolved.conf on .52). |
| 64 | **k3master (.52) post-Tailscale networking restoration (2026-05-27).** After 2026-05-26 Tailscale install + USB NIC swap on .52, the netplan config (which names interfaces by MAC) became unable to match the old MAC. Result: no `default route`, empty `[Resolve]` in `/etc/systemd/resolved.conf`, `nmcli` showed wan0 as `unmanaged`, `networkctl list` showed `unmanaged`. Pods scheduled on .52 (cloudflared, frigate-exporter) stuck in `ImagePullBackOff` (can't pull images, DNS hangs). **Fix sequence:** (1) `sudo tailscale set --accept-routes=false` on .52 (per `tailscale_mesh` rule — subnet-router must NOT accept-routes). (2) Update `/etc/netplan/00-installer-config.yaml` with current dongle MAC `c8:4d:44:35:0b:46`, add previous MAC `9c:69:d3:4a:f4:a9` as a second `wan0-prev` match with same `set-name: wan0` (allows swap-back with zero edit). (3) Configure `/etc/systemd/resolved.conf`: `[Resolve]` section with `DNS=1.1.1.1 8.8.8.8 192.168.100.1` and `FallbackDNS=9.9.9.9 149.112.112.112`. (4) `sudo systemctl restart systemd-resolved`. (5) `sudo netplan apply` — restores `default via 192.168.100.1` in `ip route`, `resolvectl status` shows links with Scopes set, wan0 transitions to `routable configured` (no longer `unmanaged`). `info.md` also updated to reflect current MAC. **Why no default route:** suspected interaction between Tailscale-install's systemd-resolved hooks + netplan-set-name-via-MAC pattern when the MAC changed (USB adapter swap). Not recurring if MAC stays current in netplan. Pairs with row 63 (pod-level DNS fix). |
| 65 | **`.62` Tailscale subnet hijack — sibling to row 64 (2026-05-28).** Twenty-four hours after `.52` was fixed, Frigate's `frigate-backup` CronJob started logging `ssh: connect to host 192.168.100.62 port 22: Operation timed out` from inside cluster pods. LAN ping from `.52`, the laptop, and the cluster all failed; Tailscale ping to `100.85.33.36` worked. Diagnosis confirmed `tailscale debug prefs` on `.62` showed `RouteAll: true` while `.62` itself was an advertised subnet router for `192.168.100.0/24`. Kernel had installed `192.168.100.0/24 dev tailscale0` in routing table 52, so reply packets to LAN peers were leaving via WireGuard while the inbound arrived via `enp1s0` — asymmetric, no return path. **Fix:** `sudo tailscale set --accept-routes=false` on `.62`. LAN inbound restored to ~0.5 ms within seconds. Approximately thirty hours of stuck `postgres-backup` jobs across `frigate`, `chickenflow`, `hydroflow`, and `face-recognition` namespaces unblocked on next scheduled run (manual catchups triggered for verification). Same gotcha already in `~/.claude/projects/-home-user/memory/feedback_cascading_faults.md` and `tailscale_mesh.md`; this row exists because the rule was applied to `.52` only the first time and `.62` was missed. **Action item for any future Tailscale subnet router added:** verify `RouteAll: false` for the subnet it advertises, on the day it advertises it. |
| 66 | **Jumphost as observability portal — Grafana on `.62`, OOB Uptime Kuma exposed, in-cluster Kuma removed (2026-05-28).** The cluster's Grafana was pinned to `k3master` via `nodeSelector`; a `.52` outage took the dashboard down with it. The cluster also ran an Uptime Kuma in the `monitoring` namespace — a watcher that depended on the thing it was watching. **Moved Grafana to a Docker container on `.62`:** `grafana/grafana:11.5.2` (NOT `:latest` — see row 67), 384 MiB memory cap, persistent bind-mount at `~/grafana/data`, provisioning files at `~/grafana/provisioning/` (datasource `cluster-prometheus` pointed at new `monitoring/prometheus-lan` LoadBalancer service `192.168.100.202:9090`; dashboards loaded from JSON on disk). Idle footprint ~210 MiB / 5–25 % CPU on the AMD C-50. Two source-controlled dashboards at `homelab-config/jumphost/grafana-dashboards/`: `homelab-overview` (seven coloured square tiles for up/down, 7-day state-timeline strip, 24 h time-series for CPU/Mem/Disk/Load) and `node-detail` (per-instance drill-down via `$instance` variable). **Exposed `.62` Uptime Kuma at `uptime.ayurforlife.eu`** by adding hostname to `cloudflared-config` ConfigMap (service `http://192.168.100.62:3001`) and a Cloudflare DNS CNAME → `<tunnel-uuid>.cfargotunnel.com`. **Put `monitor.ayurforlife.eu` behind Cloudflare Access** (was answering 200 OK to anybody) — same Access app pattern as `cams.ayurforlife.eu`, email allowlist `ive.mcfire@gmail.com`. **Deleted in-cluster Uptime Kuma** Deployment and Service in `monitoring/` namespace (`apps/monitoring/uptime-kuma.yaml@7d39bf3` removed); the `.62` Docker Kuma is now the canonical and only watcher. LB IP `192.168.100.205` freed. |
| 67 | **Grafana 12 incompatible with `.62` hardware — pin 11.5.2 (2026-05-28).** First install on `.62` used `grafana/grafana:latest` (12.4.0). Within ~30 min the container saturated its 384 MiB memory cap, swap thrashed to 396 MiB, sustained ~76 % CPU on both cores of the AMD C-50 (2011 Bobcat, 2c/1GHz/9W TDP), and every dashboard or query request was timing out at ~47 s with `context canceled` / `apiserver was unable to write a JSON response: http: Handler timeout`. Root cause: Grafana 12 ships an internal Kubernetes-style apiserver ("unified storage" / `dashboards-k8s-client`) that wants more CPU and RAM than this hardware has. **Fix:** stop + remove the container, install `grafana/grafana:11.5.2`. Idle drops to ~210 MiB and 5–25 % CPU; query latency `200 ms → 130 ms`. **Caveat:** dashboards saved into G12's unified storage do NOT survive the downgrade — restore by re-importing from JSON exports kept at `~/grafana/exported/`. Memory entry: `~/.claude/projects/-home-user/memory/feedback_grafana_jumphost.md`. Don't try this again on the C-50; if `.62` is ever replaced with hardware that has >2 cores and >4 GiB RAM, reconsider. |
| 68 | **Alertmanager: Telegram → Gmail SMTP email (2026-05-28).** The standalone Alertmanager in `monitoring/` (NOT the kube-prometheus-stack one — separate deploy) had been firing into a Telegram bot. During the 7-hour cluster outage 2026-05-27 the Telegram bot was unreachable from inside the cluster, alerts piled up. After cluster recovery a deluge of accumulated retries finally landed, but the lesson was: a notification channel that depends on the cluster being healthy is the wrong channel. **Switched the receiver** from `telegram` to `email`: `global.smtp_smarthost: smtp.gmail.com:587`, `smtp_from: ive.mcfire@gmail.com`, `smtp_auth_password_file: /etc/alertmanager/secrets/smtp-password`, `smtp_require_tls: true`. Receiver `email` with `email_configs.to: ive.mcfire@gmail.com`, `send_resolved: true`, custom subject. Critical alerts route at `group_wait: 10s` / `repeat_interval: 1h`; warning alerts use default timings; `Watchdog` and `info` severity still go to `blackhole`. **Secret:** `monitoring/alertmanager-email` with key `smtp-password`, 16-char Gmail App Password (created at `https://myaccount.google.com/apppasswords`, name auto-populated to `ive.mcfire@gmail.com`). First App Password rejected with `535 5.7.8 BadCredentials` — regenerated, second one worked. App Password recorded in `credentials.md.age` under "Gmail App Password (alertmanager)". **Source control:** deployment + ConfigMap at `homelab-config/apps/monitoring/alertmanager.yaml@bf6226c`. The legacy `alertmanager-telegram` Secret remains on disk but is no longer mounted by the Deployment — safe to delete later. |
| 69 | **ChickenFlow `postgres-backup` failing because the scheduler kept placing it on phone nodes (2026-05-28).** Investigating `KubeJobFailed` for `chickenflow-postgres-backup-*`: the same 30-hour failure window as the frigate-backup Tailscale issue, but the LAN fix in row 65 did NOT unblock chickenflow. Manual catchup of `hydroflow-postgres-backup` ran in 9 s green; the same manual catchup of `chickenflow-postgres-backup` failed in 55 s with no logs, pod deleted by the Job controller after `BackoffLimitExceeded`. Reproduced under a debug Pod (`kind: Pod` instead of `Job` to escape controller deletion): `nslookup chickenflow-postgres → ;; connection timed out; no servers could be reached`. **Root cause:** the chickenflow CronJob had no `nodeSelector`, so kube-scheduler placed it on `one6t` or `one61` ~half the time; in-pod DNS to CoreDNS at `10.43.0.10` does not work from phone nodes, so both `apk add openssh-client` (external DNS for `dl-cdn.alpinelinux.org`) and `pg_dump -h chickenflow-postgres` (internal cluster DNS) failed. Hydroflow's identical-shape CronJob had `nodeSelector: kubernetes.io/hostname: k3frigate` from day one and never saw the bug. **Fix:** added `nodeSelector: kubernetes.io/hostname: k3master` to `chickenflow-postgres-backup` CronJob (mirrors hydroflow's pattern, picked `k3master` for control-plane consistency). Manual run after the fix: 21 s, 26 K dump, scp OK. Source control at `homelab-config/apps/chickenflow/cronjob.yaml@83fed07`. Memory entry: `~/.claude/projects/-home-user/memory/feedback_phone_node_dns.md`. The underlying CNI/DNS bug on phones (likely flannel routing or kube-proxy iptables on the USB-tethered `10.0.x.x` interfaces) is not yet investigated — workaround is to pin DNS-dependent workloads off phone nodes. |
| 70 | **`CPUThrottlingHigh` was a kube-prometheus-stack false positive — removed `node-exporter` CPU limit (2026-05-28).** Six `CPUThrottlingHigh` alerts firing continuously, one per `node-exporter` DaemonSet pod. Investigation: actual CPU usage 3–20 m per pod, but CFS-throttled 52–85 % of periods. Cause: the helm chart shipped `limits.cpu: 200m` (request `50m`), and node-exporter scrapes burst above 200 m for a few milliseconds every 30 s — CFS clips the burst and the cgroup spends most of the 100 ms period throttled, even though average usage is an order of magnitude below the limit. The `for:` hold in the alert rule made the brief reality look chronic. **Fix:** `kubectl patch ds -n monitoring node-exporter --type=json -p '[{"op":"remove","path":"/spec/template/spec/containers/0/resources/limits/cpu"}]'`. `limits.memory: 64Mi` and `requests.cpu: 50m / memory: 32Mi` kept as-is. New pods rolled out clean; throttle rate dropped to zero on next sample; six stale alerts auto-resolved as cAdvisor staleness rolled the old pod-name series out of the 5-minute lookback. **Source control:** strategic-merge patch at `homelab-config/apps/monitoring/patch-node-exporter-resources.yaml`, alongside `prometheus-lan-svc.yaml`, `additional-scrape-configs.yaml`, `homelab-alerts-prometheusrule.yaml`, and a README documenting the larger drift — `kube-prometheus-stack` is no longer tracked by Helm in this cluster (`helm list -n monitoring` shows only `loki`), so the chart's defaults are not the live state. Re-adopting the stack under Helm is a future task. |
| 71 | **Manifest consolidation — `homelab-config` becomes the single IaC source; per-app repos archived (2026-07-12).** Trigger was a `KubeJobFailed` email for `frigate-backup`: rsync exited 24 ("some files vanished") because Frigate's retention pruning deleted two recording segments mid-sync. The retry pod succeeded, so data was never at risk — the alert was noise. Fixed by tolerating rsync exit 23/24 in the CronJob script (`homelab-config/frigate-backup/backup-cronjob.yaml`); any other non-zero exit still fails the job. Verified with a manual Job run (full incremental sync, clean exit). The fix surfaced the larger housekeeping debt: manifests lived in per-app git repos (`~/frigate-k8s`, `~/face-recognition`, `~/double-take`, `~/monitoring-stack`, `~/sidecar-manifests`) plus loose YAMLs in `~`, most drifted from or duplicating `homelab-config`. **Consolidated:** full Frigate deployment manifests (deployment/configmap/cloudflared/rbac/svc/ns) folded into `homelab-config/apps/frigate/` — working tree verified against live cluster (image `0.17.1-tensorrt`, CPU limit 3) and secret-grepped before landing in the public repo (cam passwords are empty or env-substituted from Secret `frigate-rtsp-creds`; cloudflared token via Secret volume); final drift committed to the retired `frigate-k8s` repo as `340ab59` first. The Frigate ConfigMap in git remains bootstrap-only — hostPath `/mnt/frigate/config/config.yml` on k3frigate stays authoritative. Rescued `grafana-dashboard-frigate.json` (existed nowhere else) → `apps/monitoring/grafana-dashboards/`. Dropped the stale `apps/gitea.yaml` monolith (`gitea:latest`); the `apps/gitea/` split (pinned `1.22.6`, matches live) is canonical. **Archived to `.52:~/attic-2026-07-12/`:** the five per-app repo dirs (face-rec + double-take retired with their namespaces; git remotes retain history) plus March-era hydroflow build artifacts, `INFRA.md.bak`, dead one-off YAMLs (`hello-phones`, `kuma` — neither in cluster), and a stale Feb k3s arm64 binary. Delete the attic after a few weeks if nothing is missed. Deleted outright: `k3s-ready` (byte-identical dup of the arm64 binary), stale `new-node-token.txt` (re-readable from `/var/lib/rancher/k3s/server/node-token`), misc junk files. Closes the stale-frigate-k8s-in-git follow-ups from rows 40, 57, and 58. Source control: `homelab-config@1bc6be3` pushed to GitHub (gitea `origin` is a read-only mirror and syncs itself). |
